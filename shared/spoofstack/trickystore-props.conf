# TrickyStore L1 property-spoof set — CLO-12
# Loaded by install-trickystore.sh when SPOOFSTACK_L3=trickystore.
# These values impersonate a Pixel 8 (husky) running Android 14 QPR3.
# All six props required by the buildprop.fingerprint probe (rank #1).
#
# Format: <prop>=<value>
# Processed by: adb shell magisk --module-prop or resetprop inside the module's
# post-fs-data.sh service entry.

ro.build.fingerprint=google/husky/husky:14/AP2A.240905.003/12231197:user/release-keys
ro.product.model=Pixel 8
ro.product.brand=google
ro.product.manufacturer=Google
ro.build.tags=release-keys
ro.build.type=user
